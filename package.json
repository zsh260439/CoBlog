{
  "name": "co-blog",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --workspace=client\" \"npm run start:dev --workspace=server\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run start:dev --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server"
  },
  "dependencies": {
    "echarts": "^6.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
