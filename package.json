{
  "name": "co-blog",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"pnpm --filter dev-blog-client dev\" \"pnpm --filter server start:dev\"",
    "dev:client": "pnpm --filter dev-blog-client dev",
    "dev:server": "pnpm --filter server start:dev",
    "build": "pnpm --filter dev-blog-client build && pnpm --filter server build"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.10",
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7"
  }
}
